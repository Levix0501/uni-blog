import RevalidateCard from '@/components/admin/revalidate-card';

const Page = async () => {
	// generate();
	return (
		<div className="flex flex-col gap-4">
			<RevalidateCard />
		</div>
	);
};

export default Page;
